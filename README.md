# CineBook

A full-stack cinema ticket-booking platform (Next.js + TypeScript + Prisma), built to run entirely on a local development machine.

> **Status: work in progress.** This README is a placeholder during active development and will be replaced with full setup/usage documentation (installation, environment configuration, default accounts, testing, deployment notes) once the core booking flow is complete. See `docs/` for architecture notes as they land.
